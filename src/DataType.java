// Enum used to represent data of different types passed by the view to
// its caller (Controller). 
public enum DataType {
	INT,	// represent generic integers
	STRING, // represent string data that can contain a white space
	FLOAT;	// represents floating point numbers
}
